# --- Dependencies ---
node_modules/
**/node_modules/
package-lock.json
yarn.lock
# (Note: Only ignore lock files if you intentionally don't want to track versions. Usually, it's better to KEEP lock files, so you can delete these two lines if you want version consistency.)

# --- Environment Variables (CRITICAL) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
**/.env
**/.env.*

# --- Build Outputs (Frontend) ---
dist/
build/
**/dist/
**/build/

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs
*.log
npm-debug.log.*

# --- IDEs and Editors ---
.vscode/
.idea/
*.swp
*.swo

# --- OS Files ---
.DS_Store
Thumbs.db
Desktop.ini

# --- Vercel ---
.vercel
**/.vercel

# --- Testing ---
coverage/
**/coverage/

# --- Optional: Ignore git folders inside submodules if you pasted them ---
client/.git
server/.git